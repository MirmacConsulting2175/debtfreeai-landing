version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
      - ../public/**/*
      - ../package.json
      - ../next.config.*
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
      - node_modules/**/*